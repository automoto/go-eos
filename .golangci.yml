version: "2"

linters:
  enable:
    - errcheck
    - govet
    - staticcheck
    - unused
    - ineffassign

  settings:
    errcheck:
      check-blank: false

  exclusions:
    paths:
      - eos/internal/cbinding
    rules:
      - linters:
          - errcheck
        path: _test\.go
